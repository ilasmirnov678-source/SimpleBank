<Window x:Class="SimpleBank.View.BankAccountsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        mc:Ignorable="d"
        Title="Работа с банковскими счетами" 
        Height="700" 
        Width="1000"
        MinHeight="650"
        MinWidth="900"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Resources/icon.ico"
        Closing="Window_Closing">
    <Grid Background="{StaticResource WhiteBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок с кнопкой "Назад" -->
        <Border Grid.Row="0" 
                Background="{StaticResource MatteBlueBrush}" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="Работа с банковскими счетами"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="{StaticResource WhiteBrush}"
                           VerticalAlignment="Center"/>
                
                <Button Grid.Column="1"
                        Name="btnBack"
                        Content="Назад"
                        Style="{StaticResource BackButtonStyle}"
                        Click="btnBack_Click"
                        Width="100"/>
            </Grid>
        </Border>
        
        <!-- Область с логотипом -->
        <Border Grid.Row="1" 
                Background="{StaticResource MatteLightBlueBrush}" 
                Padding="20,20">
            <StackPanel HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/Resources/logo.png" 
                       Width="160" 
                       Height="80"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
                <TextBlock Text="Управление банковскими счетами"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource MatteDarkBlueBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Основная область с секциями для двух счетов -->
        <ScrollViewer Grid.Row="2" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- Секция для первого счета -->
                <Border Grid.Column="0"
                        BorderBrush="{StaticResource MatteBlueBrush}"
                        BorderThickness="2"
                        CornerRadius="8"
                        Padding="20"
                        Background="{StaticResource MatteLightBlueBrush}">
                    <StackPanel>
                        <TextBlock Text="Счет 1"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource MatteDarkBlueBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,20"/>
                        
                        <!-- Группа "Информация о счете" -->
                        <GroupBox Header="Информация о счете"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource MatteDarkBlueBrush}"
                                  Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Номер счета:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount1Number"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Дата открытия:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <DatePicker Name="dpAccount1OpeningDate"
                                            FontSize="12"
                                            Padding="8,5"
                                            Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Владелец:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <ComboBox Name="cmbAccount1Owner"
                                          FontSize="12"
                                          Padding="8,5"
                                          Height="35"
                                          Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Начальный баланс:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount1Balance"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Срок вклада (дни):" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount1DepositTerm"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Статус:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <ComboBox Name="cmbAccount1Status"
                                          FontSize="12"
                                          Padding="8,5"
                                          Height="35"
                                          Margin="0,0,0,10"/>
                                
                                <Button Name="btnAccount1Create"
                                        Content="Создать/Обновить счет"
                                        Style="{StaticResource MenuButtonStyle}"
                                        FontSize="14"
                                        Padding="15,10"
                                        Height="40"
                                        Click="btnAccount1Create_Click"
                                        Margin="0,10,0,0"/>
                            </StackPanel>
                        </GroupBox>
                        
                        <!-- Группа "Операции" -->
                        <GroupBox Header="Операции"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource MatteDarkBlueBrush}"
                                  Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Сумма операции:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount1Amount"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="5"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <Button Grid.Row="0" Grid.Column="0"
                                            Name="btnAccount1Deposit"
                                            Content="Пополнить"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount1Deposit_Click"/>
                                    
                                    <Button Grid.Row="0" Grid.Column="2"
                                            Name="btnAccount1Withdraw"
                                            Content="Снять"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount1Withdraw_Click"/>
                                    
                                    <Button Grid.Row="2" Grid.Column="0"
                                            Name="btnAccount1Zero"
                                            Content="Обнулить"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount1Zero_Click"/>
                                    
                                    <Button Grid.Row="2" Grid.Column="2"
                                            Name="btnAccount1Bankrupt"
                                            Content="Проверить банкрот"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount1Bankrupt_Click"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                        
                        <!-- Группа "Текущее состояние" -->
                        <GroupBox Header="Текущее состояние"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource MatteDarkBlueBrush}"
                                  Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock>
                                    <Run Text="Номер счета: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount1Number" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Баланс: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount1Balance" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Статус: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount1Status" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Дата открытия: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount1OpeningDate" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Дата закрытия: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount1ClosingDate" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Дней до закрытия: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount1DaysLeft" Text="—"/>
                                </TextBlock>
                                
                                <Button Name="btnAccount1Refresh"
                                        Content="Обновить информацию"
                                        Style="{StaticResource MenuButtonStyle}"
                                        FontSize="12"
                                        Padding="15,8"
                                        Height="35"
                                        Click="btnAccount1Refresh_Click"
                                        Margin="0,10,0,0"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </Border>
                
                <!-- Разделитель -->
                <Border Grid.Column="1" 
                        Background="{StaticResource MatteBlueBrush}"
                        Width="2"/>
                
                <!-- Секция для второго счета -->
                <Border Grid.Column="2"
                        BorderBrush="{StaticResource MatteBlueBrush}"
                        BorderThickness="2"
                        CornerRadius="8"
                        Padding="20"
                        Background="{StaticResource MatteLightBlueBrush}">
                    <StackPanel>
                        <TextBlock Text="Счет 2"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource MatteDarkBlueBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,20"/>
                        
                        <!-- Группа "Информация о счете" -->
                        <GroupBox Header="Информация о счете"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource MatteDarkBlueBrush}"
                                  Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Номер счета:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount2Number"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Дата открытия:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <DatePicker Name="dpAccount2OpeningDate"
                                            FontSize="12"
                                            Padding="8,5"
                                            Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Владелец:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <ComboBox Name="cmbAccount2Owner"
                                          FontSize="12"
                                          Padding="8,5"
                                          Height="35"
                                          Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Начальный баланс:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount2Balance"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Срок вклада (дни):" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount2DepositTerm"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <TextBlock Text="Статус:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <ComboBox Name="cmbAccount2Status"
                                          FontSize="12"
                                          Padding="8,5"
                                          Height="35"
                                          Margin="0,0,0,10"/>
                                
                                <Button Name="btnAccount2Create"
                                        Content="Создать/Обновить счет"
                                        Style="{StaticResource MenuButtonStyle}"
                                        FontSize="14"
                                        Padding="15,10"
                                        Height="40"
                                        Click="btnAccount2Create_Click"
                                        Margin="0,10,0,0"/>
                            </StackPanel>
                        </GroupBox>
                        
                        <!-- Группа "Операции" -->
                        <GroupBox Header="Операции"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource MatteDarkBlueBrush}"
                                  Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock Text="Сумма операции:" 
                                           FontSize="12"
                                           Margin="0,5,0,5"/>
                                <TextBox Name="txtAccount2Amount"
                                         Style="{StaticResource InputTextBoxStyle}"
                                         Height="35"
                                         Margin="0,0,0,10"/>
                                
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="5"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="5"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    
                                    <Button Grid.Row="0" Grid.Column="0"
                                            Name="btnAccount2Deposit"
                                            Content="Пополнить"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount2Deposit_Click"/>
                                    
                                    <Button Grid.Row="0" Grid.Column="2"
                                            Name="btnAccount2Withdraw"
                                            Content="Снять"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount2Withdraw_Click"/>
                                    
                                    <Button Grid.Row="2" Grid.Column="0"
                                            Name="btnAccount2Zero"
                                            Content="Обнулить"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount2Zero_Click"/>
                                    
                                    <Button Grid.Row="2" Grid.Column="2"
                                            Name="btnAccount2Bankrupt"
                                            Content="Проверить банкрот"
                                            Style="{StaticResource MenuButtonStyle}"
                                            FontSize="12"
                                            Padding="10,8"
                                            Height="35"
                                            Click="btnAccount2Bankrupt_Click"/>
                                </Grid>
                            </StackPanel>
                        </GroupBox>
                        
                        <!-- Группа "Текущее состояние" -->
                        <GroupBox Header="Текущее состояние"
                                  FontSize="14"
                                  FontWeight="SemiBold"
                                  Foreground="{StaticResource MatteDarkBlueBrush}"
                                  Margin="0,0,0,15">
                            <StackPanel Margin="10">
                                <TextBlock>
                                    <Run Text="Номер счета: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount2Number" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Баланс: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount2Balance" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Статус: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount2Status" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Дата открытия: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount2OpeningDate" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Дата закрытия: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount2ClosingDate" Text="—"/>
                                </TextBlock>
                                <TextBlock Margin="0,5,0,0">
                                    <Run Text="Дней до закрытия: " FontWeight="SemiBold"/>
                                    <Run Name="lblAccount2DaysLeft" Text="—"/>
                                </TextBlock>
                                
                                <Button Name="btnAccount2Refresh"
                                        Content="Обновить информацию"
                                        Style="{StaticResource MenuButtonStyle}"
                                        FontSize="12"
                                        Padding="15,8"
                                        Height="35"
                                        Click="btnAccount2Refresh_Click"
                                        Margin="0,10,0,0"/>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </Border>
            </Grid>
        </ScrollViewer>
        
        <!-- Подвал -->
        <Border Grid.Row="3" 
                Background="{StaticResource MatteLightBlueBrush}" 
                Padding="20,15">
            <TextBlock Text="Банковское приложение SPBKIT Bank"
                       FontSize="12"
                       Foreground="{StaticResource MatteDarkBlueBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>

