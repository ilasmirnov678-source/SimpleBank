<Window x:Class="SimpleBank.View.UserInformationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Информация для клиента" 
        Height="650" 
        Width="850"
        MinHeight="600"
        MinWidth="750"
        WindowStartupLocation="CenterScreen"
        Icon="pack://application:,,,/Resources/icon.ico"
        Closing="Window_Closing">
    <Grid Background="{StaticResource WhiteBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Заголовок с кнопкой "Назад" -->
        <Border Grid.Row="0" 
                Background="{StaticResource MatteBlueBrush}" 
                Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                           Text="Информация для клиента"
                           FontSize="22"
                           FontWeight="Bold"
                           Foreground="{StaticResource WhiteBrush}"
                           VerticalAlignment="Center"/>
                
                <Button Grid.Column="1"
                        Name="btnBack"
                        Content="Назад"
                        Style="{StaticResource BackButtonStyle}"
                        Click="btnBack_Click"
                        Width="100"/>
            </Grid>
        </Border>
        
        <!-- Область с логотипом -->
        <Border Grid.Row="1" 
                Background="{StaticResource MatteLightBlueBrush}" 
                Padding="20,20">
            <StackPanel HorizontalAlignment="Center">
                <Image Source="pack://application:,,,/Resources/logo.png" 
                       Width="160" 
                       Height="80"
                       Stretch="Uniform"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
                <TextBlock Text="Данные для пользователя"
                           FontSize="24"
                           FontWeight="Bold"
                           Foreground="{StaticResource MatteDarkBlueBrush}"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
        
        <!-- Основная область с информацией -->
        <Grid Grid.Row="2" 
              Margin="40,30"
              VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Добро пожаловать в информационный раздел банка!"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource MatteDarkBlueBrush}"
                           TextWrapping="Wrap"
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"/>
                
                <TextBlock Text="Если бы это был настоящий банк, то здесь была бы размещена информация для клиента:"
                           FontSize="14"
                           Foreground="#666666"
                           TextWrapping="Wrap"
                           Margin="0,0,0,15"/>
                
                <TextBlock Text="• Просмотр баланса счетов&#x0a;• История операций и транзакций&#x0a;• Информация о вкладах и кредитах&#x0a;• Курсы валют&#x0a;• Контактная информация банка"
                           FontSize="14"
                           Foreground="#666666"
                           TextWrapping="Wrap"
                           Margin="20,0,0,0"/>
            </StackPanel>
        </Grid>
        
        <!-- Подвал -->
        <Border Grid.Row="3" 
                Background="{StaticResource MatteLightBlueBrush}" 
                Padding="20,15">
            <TextBlock Text="Банковское приложение SPBKIT Bank"
                       FontSize="12"
                       Foreground="{StaticResource MatteDarkBlueBrush}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
